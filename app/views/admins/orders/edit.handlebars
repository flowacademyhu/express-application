<div class="col-4 p-3 m-3 mx-auto bg-dark text-white">
<form method="POST" action="/admin/orders/{{order.id}}?_method=put">
 <div class="btn-group col-md-2 ml-1"><h4>ID: {{order.id}}</h4></div>

{{!--   <div class="form-group">
    <label for="">Status:</label>
    <input name="status" type="options" value="{{order.status}}" class="form-control">
  </div> --}}

 <div class="form-group">
    <label for="status">Status:</label>
 {{!--    <input name="status" type="options" value="{{order.status}}" class="form-control"> --}}
    

{{!--     <div class="dropdown">
    <label>Categories:</label>
    <select class="ui dropdown" name="status">
      {{#each order}}
        <option value="{{id}}">{{status}}</option>
      {{/each}}
    </select>
  </div> --}}

  <select class="ui dropdown" name="status" form="form-group">
    {{#each statuses}}
      <option value="{{value}}" {{#if isSelected}} selected {{/if}}>{{label}}</option>
    {{/each}}
    </select>
  </div> 


  <div class="form-group">
    <label for="">User ID:</label>
    <input name="price" type="text" value="{{order.userId}}" class="form-control">
  </div>

  
  <button class="btn btn-success my-2 my-sm-0" type="submit" onclick="?_method=put&status=ordered">Edit</button>
</form>
</div>


